> [!NOTE]
> **Current architecture interpretation — 2026-09-18**
> - **Subject class:** `DECISION`
> - **Role:** Reflective Continuum architecture decision ADR-009
> - **Authority:** Current repository-native design rationale for the behavior, boundary, or evidence rule explicitly owned by this ADR
> - **Current meaning:** Interpret this ADR through current runtime mechanics and current Specification; conceptual labels never outrank directly observed implementation
> - **Evidence / implementation boundary:** The decision does not itself prove execution, persistence, convergence, semantic understanding, or historical continuity outside the cited evidence scope
> - **Cross-document relation:** Specification and runtime code own implementation facts; Methodology owns procedures; dated evidence owns point-in-time observations
> - **Update trigger:** Update when the owned decision changes or current implementation/evidence invalidates its stated boundary
> - **Preservation rule:** Existing subject history and dated examples retain their original time boundary. This pass clarifies current interpretation and corrects only confirmed current-authority drift

# Evaluation and claim scope follows the exact evidence surface

- Decision date: 2026-08-05
- Review calibration: 2026-09-18
- Status: Accepted

## Status

Accepted for the bounded reference implementation; broader system claims remain out of scope.

## Context

Reflective Continuum exposes multiple evidence surfaces that answer different questions. Stable hashes, successful selfchecks, local ingestion acceptance, lexical drift results, graph entropy, and Weekly task summaries are not interchangeable.

## Decision

Keep these evidence classes separate:

- database initialization / SQLite integrity
- foreign-key and FTS5 accessibility
- transaction commit or rollback
- snapshot digest identity
- fixed-fixture repeatability
- structural graph delta
- lexical top-result delta
- PageRank score delta
- graph-derived Shannon entropy
- rule-engine acceptance
- source-to-claim support
- durable persistence continuity
- historical task execution state

Examples:

- `cortex_selfcheck.py` supports only its named checks for the database it opened
- `convergence_drill.py` supports only repeatability of its fixed local SQLite fixture
- `semantic_drift_audit.py` supports selected-version structural/lexical comparisons for caller queries
- `InsightMorpher` acceptance supports only local ingestion/control-flow outcome
- a snapshot digest supports snapshot identity, not persistence through an unobserved interval

## Historical aggregation rule

A higher-level R3/R4/R5 artifact does not erase Daily R1/R2 failures, errors, rejected signals, missing runtime evidence, or `NOT_COMPUTED` dimensions.

Current retained August evidence therefore preserves:

- 2026-08-07 through 2026-08-10 R2 `26 passed / 1 error`
- 2026-08-17 through 2026-08-27 R2 `26 passed / 1 failed`
- 2026-08-06 original R2 runtime as `HISTORICAL_RUNTIME_UNKNOWN`

The 2026-08-17 through 2026-08-27 range is a historical evidence range, not a claim that the same test state persisted after that cutoff. Later Daily/Weekly/Monthly records retain their own dates and evidence.

## Consequences

Callers receive an inspectable local contract but remain responsible for deployment, authority, persistence, and claims outside the named implementation surface.

## Verification

Verification requires a named repository path, revision, fixture or store identity, command, environment, exit code, and retained failures. Documentary review alone is not runtime or semantic validation.

## Evidence boundary

A successful observation supports the exact checked property and scope only.

It does not automatically establish semantic truth, alignment, safety, durable memory, global stability, or convergence.
