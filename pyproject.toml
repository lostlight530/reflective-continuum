[project]
name = "reflective-continuum"
version = "0.1.0"
description = "Versioned SQLite graph and bounded reflection reference contracts"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.coverage.run]
branch = true
source = ["CODE"]